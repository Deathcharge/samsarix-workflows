[build-system]
requires = ["uv_build==0.12.5"]
build-backend = "uv_build"

[project]
name = "samsarix-uv-fixture"
version = "0.0.0"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest==9.1.1"]

[tool.uv]
required-version = "==0.12.0"
